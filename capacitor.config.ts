import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'u21503193_hw02',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
